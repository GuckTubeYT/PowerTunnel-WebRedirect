rootProject.name = 'PowerTunnel-WebRedirect'

sourceControl {
    gitRepository(new URI('https://github.com/krlvm/PowerTunnel.git')) {
        producesModule('io.github.krlvm.powertunnel:sdk')
    }
}